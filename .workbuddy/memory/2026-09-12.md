# 2026-09-12

## 频谱仪切换/兼容 FSW（R&S FSW User Manual 1173.9411.02 ─ 56，本地 PDF 1615 页）

- 杂散程序迹线最大保持指令改为 FSW 原生形式：
  - `instruments/spectrum_analyzer.py::set_trace_mode`：`TRAC:MODE {mode}` → `DISP:WIND:TRAC:MODE {mode}`
  - 依据：手册 p1245 `DISPlay[:WINDow<n>][:SUBWindow<w>]:TRACe<t>:MODE <Mode>`；p911/p912 示例 `DISP:WIND:TRAC:MODE AVER`
  - 旧写法 `TRAC:MODE` 只存在于手册 "Supported 89600 commands"（p1528），属 89600 仿真指令集，标准频谱模式不生效
  - 调用方仅 `procedures/spurious_procedure.py::_configure_sa`（谐波/小信号不走该分支）
- **已回退（2026-09-12 17:43，用户要求）**：上述 `set_trace_mode` 改动已还原为 `TRAC:MODE {mode}` + 原 docstring。
  - 注意：`instruments/spectrum_analyzer.py` 内还有大量用户自己的未提交改动（`wait_for_sweep_fast` 新增、`set_scale_div` 删除、`set_attenuation` 改用 `INP:ATT`、`wait_for_sweep` 新增 factor/margin）。
  - **禁止对该文件用 `git checkout --`**，会连同用户未提交改动一起冲掉。
- 待确认（未改）：`set_detector` 用的 `DET:TRAC` 在 FSW 基础单元指令表中不存在。
  - 依据：手册 p1250/1251 只有 `[SENSe:][WINDow<n>:]DETector<t>[:FUNCtion]` 与 `:FUNCtion:AUTO`；
    "List of Commands (base unit)"（p1561）中 DETector 条目仅这 2 条；全文检索 "DET:TRAC"/"DETector<t>:TRACe" 零命中
  - 建议改为 `DET {detector}`（手册示例 `DET POS`）
- FSW 可用但当前未用的能力：
  - `[SENSe:]SWEep:COUNt`（p912）→ FSW 支持 sweep 计数，可与 `INIT;*WAI` 同步
  - `DISPlay[:WINDow<n>][:SUBWindow<w>]:TRACe<t>:MODE:HCONtinuous`（p1246）→ 控制参数变更后 max hold/min hold/average 的自动清零
  - `INITiate<n>[:IMMediate]` 在 MAXHold/MINHold/AVERage 下会清零历史结果；`INITiate<n>:CONMeas` 则保留（p911）
- 环境提示：本机 bash 的 PATH 异常，`ls`/`head`/`dirname` 不可用；改用 managed Python 绝对路径 + PowerShell 执行命令。
  PDF 检索脚本落在 `%TEMP%\pdf_search.py` / `pdf_find.py` / `pdf_dump.py`。
