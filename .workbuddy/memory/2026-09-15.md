# 2026-09-15

- 修正小信号测量所用频谱仪驱动，依据 R&S FSWP-B1 User Manual (1177.5656.02 ─ 11)。
  - 文件：`instruments/spectrum_analyzer.py`、`configs/small_signal_config.py`
  - 主要变更：参考电平、衰减、预放、峰值搜索、迹线模式、检波器改为 FSWP-B1 指令；保留对旧简写（MAXH/WRITE/POS 等）和预放 band（LOW/FULL）的兼容映射。
- 通过 R&S 官方 CDN 下载 FSWP-B1 手册 PDF 到 `output/FSWP_B1_UserManual_en_11~1.pdf` 并提取相关 SCPI 章节用于核对。
- 确定多品牌 SCPI 适配方案（仅频谱仪）：门面 + 品牌后端类，品牌自动识别（*IDN?）可覆盖，品牌先覆盖 R&S + Keysight。等用户提供是德型号/手册后开工。
- 用户提供的 `N9030-MANUAL`（资料库节点 QoKRbP0nSvRCQcAflgjpxu）实为 **X-Series Analog Demod Mode** 分册（1413 页），不含 Swept SA 指令（全书无 `DETector`/`TRACe:MODE`/`BWIDth:VIDeo`，span/RBW 为 `:AM:`/`:FM:` 前缀）。已向用户索取正确的 **Spectrum Analyzer Mode** 分册（Keysight pub N9060-90041）。
- 用户补齐正确的 `N9030B-SA-MANUAL.pdf`（N9060-90041 Ed.19，3048 页），逐项核对 Swept SA 指令后完成多品牌重构：
  - 新增 `instruments/brand.py`、`instruments/backends/{__init__,sa_base,sa_rohde,sa_keysight}.py`；
  - 改写 `instruments/spectrum_analyzer.py` 为门面（`brand=` 可覆盖），方法签名与旧版完全一致；
  - `README.md` 增补多品牌适配小节。
  - 冒烟测试 5 场景（R&S / Keysight / Agilent / 未知 / 显式覆盖）全部通过；`compileall` 全项目通过。
- **适配范围最终定案（用户明确）**：信号源不做多品牌适配 —— 信号源是**被测件（DUT）**，现场只接自家机型，无需品牌分支。结论推广：**只有"测量仪器"才需要多品牌**。已在 `README.md` 适配章节顶部与目录树标注该范围。
- 审查 `procedures/spurious_procedure.py`（杂散测量）的负频率防护与谐波判定，结论：两者都有实现但均不完整。待用户确认后修复的 3 处：
  1. `_build_far_segment_centers`（L602-635）只校验**段中心** `center <= 0`，未校验**段下边缘** `center - span/2`。因 center>0 ⟺ 下边缘 > -span/2，故中心落在 (0, span/2) 的段会带着负频率下边缘下扫。当前 config（CF≥1 GHz、coverage 2 GHz、span 1 GHz）恰好不触发，属潜伏缺陷。
  2. `_measure_harmonics`（L640-698）在 n·CF 处 10 MHz 窗口内 `peak_search` 后**直接硬编码** `is_harmonic=True`（L695），未校验实测峰频 `|peak_freq - n·CF| <= tolerance`；窗口内的非谐波强杂散会被误标为谐波。
  3. `_scan_ambient`（L472）引用了未定义变量 `segment_name`（该函数内无此变量，只有 `segment`）→ 一旦环境扫描同步失败即 `NameError`。当前 `source_off_check=False` 未触发。
- 另注：`output.include_harmonics = False`（config L155），且远载波只覆盖 CF ±1 GHz，故**当前配置下谐波实际上基本不会被捕获**（H2 在 CF>1 GHz 时超出 ±1 GHz 覆盖范围）。`max_frequency_hz = 20e9` 被同时用作谐波上限，而载波列表到 40 GHz（config L15）。
- 通盘清点 `spurious_config.py` 全部参数（用户要"斟酌细节参数"），发现以下需处理的配置问题：
  - **死参数（写了但代码从不读）**：`project_name`、`validation.attenuator_step_db`（实际只读 `attenuator_steps`）、`validation.stability_count`（L369 赋值后再未使用，真正决定平均次数的是 `refine_config.average_count`）；`validation.cf_step_check` / `cf_step_hz` **功能整体未实现**。
  - **参数被架空**：`min_spur_offset_hz = 5e3` 在 `_scan_segment`（L551-557）中被 `carrier_guard_hz = 10e3` 完全支配（guard 已先砍掉 ≤10 kHz 的候选），该分支永不触发。
  - **语义漂移**：`cf_step_freq_tolerance_hz` 名为"载波步进容差"，实际被环境杂散比对复用（L423）。
  - **频率边界三套口径混用**：`carrier_end_frequency_hz=40e9`（扫描用）/ `max_frequency_hz=20e9`（谐波上限）/ `min_frequency_hz=9e3`（仅用于值级过滤与段中心校验）。
  - **名实不符**：`far_carrier_segment` 注释称"1 GHz 分段覆盖全频段"，但 coverage 2e9 + span 1e9 实际只产出 **2 段（CF ±1 GHz）**。
  - 两级噪声门限并存：`peak_detection.noise_margin_db=6`（入围）与 `validation.noise_floor_reliable_margin_db=10`（保留）。
- **按要求完成杂散配置清理 + 频率边界修复**（`configs/spurious_config.py`、`procedures/spurious_procedure.py`）：
  - 删死参数：`project_name`、`validation.attenuator_step_db`、`validation.stability_count`（及其赋值语句）。
  - 删未实现功能：`validation.cf_step_check`、`cf_step_hz`。
  - 修"架空"：`min_spur_offset_hz` **此前根本没被 `get_config()` 导出**，代码永远走 `.get(..., 5e3)` 默认值 → 已导出；并重新定义为**事后**按实测峰频的终判（`carrier_guard_hz` 保留为**事前**按候选频率剪枝），两者分工不互相覆盖，值统一 10 kHz。
  - 语义漂移修正：`cf_step_freq_tolerance_hz` → `ambient_freq_tolerance_hz`。
  - 噪声门限统一为**唯一**的 6 dB（`peak_detection.noise_margin_db`），删除 `noise_floor_reliable_margin_db`（原 10 dB）；`_scan_segment` 保留门槛改读该参数。
  - 频率口径统一：`max_frequency_hz` 20e9 → **40e9**（载波最高测 40 GHz），同时是远段上限与谐波上限。
  - `_build_far_segment_centers` 重写：边界校验由"段中心"改为"**段上下边缘**"，且越界段**向内夹逼**（而非整段丢弃）+ 重合去重。原因：只校验中心时 center>0 仅等价于"下边缘 > -span/2"；且 40 GHz 载波原会生成 40~41 GHz 越界段。修复后 1 GHz 载波的下半段仍能覆盖 9 kHz~1 GHz（旧严格版会整段丢失，视为回归）。
  - 顺手修 `_scan_ambient` 的 `NameError`（L472 引用未定义的 `segment_name`）。
  - 验证：6 个载波点位（1/0.9/1.2/20/39.5/40 GHz）段边界全部合法；`compileall` 通过。谐波实测保持关闭（`output.include_harmonics=False`），仅保留谐波**标记**。
  - 遗留待议：`far_carrier_segment.coverage_hz=2e9` 只覆盖 CF ±1 GHz（非全频段），是否需扩大未决。
- 盘点全项目输出字段（6 个产出源 + GUI + 历史遗留），结论：**列名体系三套并存、互不兼容**。
  - 产出源与列数：小信号 14 / 杂散 15 / 谐波 16 / 分谐波 7 / 功率扫描 6 / 最大功率摘要 8 + 明细 7；GUI 直接 dump 结果 dict 无固定表。
  - 主要不一致：① 频率列 `frequency` vs `frequency_hz` vs `frequency_mhz`（后者还是冗余重复列）；② 功率列 `set_power` vs `set_power_dbm`、`measured_power` vs `measured_power_dbm` vs `amplitude_dbm`；③ 频谱仪参数前缀 `span_hz` vs `sa_span_hz`，且 `sa_reference_level_db` 漏了 m（应为 dBm）；④ `timestamp` 列序首/末不一；⑤ `status` 取值域各不相同；⑥ 输出载体不统一（小信号/谐波/分谐波 转 xlsx 后删 CSV；杂散保留 CSV）；⑦ 摘要 sheet 只有功率类有，且 `power_sweep_base` 内部两条路径的「最大功率摘要」一个 8 列一个 9 列（后者多「频率显示」）；⑧ `small_signal` 的 `attenuation_db` 列混入字符串 `"AUTO"`；⑨ 分谐波 fieldnames 硬编码 `subharmonic_2_*`（orders 现为 [2]，改阶数即崩）；⑩ 历史 CSV 为 GBK + 中文表头（测量频率/设定功率/实际功率/timestamp）。
  - 未改动任何代码，等用户确认统一口径后再落地。
- **输出格式定案（用户决定）**：**只出 CSV**，不再生成 xlsx。理由链：xlsx 是 zip 容器、尾部 EOCD 必须最后写 → 无法真流式、半成品打不开；长跑测试中途崩溃只丢最后一段的需求由 CSV 满足。数据库（SQLite）留待后续，届时 CSV 退化为导出格式。
  - 该决定尚未落地为代码改动（用户明确"还没到执行的时候"）。**执行时与字段统一同批做**，避免同一批文件改两遍、回归做两次。
  - 附带影响：`power_sweep_base` / `power_sweep_procedure` 自建 openpyxl 工作簿（中文表头、列宽）与 `gui.py::_save_results` 的 xlsx 导出都要一并去掉；`save_results_to_excel` / `finish_xlsx` / `to_xlsx` 三条路径失效。
- 字段设计讨论（未定案）中发现的**最严重问题**：同名列 `frequency_hz` 在不同源里语义相反 ——
  - 杂散：`frequency_hz` = **杂散频点**，测点是 `carrier_frequency_hz`；
  - 谐波/分谐波/小信号：`frequency_hz`（或 `frequency`）= **测点频率**。
  直接合并分析会得出错误结论，必须在改名时拆成 `frequency_hz`（固定=测点）+ `meas_frequency_hz`（实际观测频点）。
- 已定：**不要中文表头**（用户："这个本来就不重要"）→ 连带 `frequency_mhz` 这类纯展示列可删。
- 各源**行粒度不同**（一行代表什么）：小信号 = 测点×功率档；杂散 = 载波×杂散候选；谐波/分谐波 = 测点×阶数；功率扫描 = 测点×功率档；最大功率摘要 = 测点。故不能强做单一宽表，进库应按粒度分表。
- **字段规范定案（用户逐条确认，尚未写代码）**：
  - 主信号频率字段统一为 **`carrier`**（原 `frequency` / `frequency_hz` / `carrier_frequency_hz` 全部废弃）。
  - 杂散频点字段为 **`spurious_freq`**（原 `frequency_hz`）。
  - A 区偏差项合并：`delta_db` + `delta_ref`（`set_power` / `carrier`），吸收原 `error_db` / `relative_dbc` / `harmonic_suppression_dbc` / `subharmonic_N_suppression_dbc`。
  - 行粒度不做统一（各表内容本就不同）。
  - B 区保持 `sa_` 前缀（只会用到频谱仪，无需多仪器抽象）。
  - 谐波/分谐波改**长表**：加 `order` 单列，废弃 `subharmonic_2_*` 写死列名。
  - 不要中文表头；`frequency_mhz` 等纯展示列删除。
  - 最大功率的「明细 + 摘要」两表形式用户满意，改用**两个 CSV**：`<tag>_detail_<ts>.csv`（每步进一行）+ `<tag>_summary_<ts>.csv`（每测点一行）。代码已有 CSV 分支（`power_sweep_base.py` L183-209），但当前入口传 .xlsx 故从未执行。
- **更正上一轮的错误结论**：并非"小信号/谐波/分谐波都删中间 CSV"。实测只有 `small_signal_procedure` 会删（`spurious_procedure` 的删除只在从未被调用的 `finish_xlsx` 里）；谐波/分谐波/最大功率/功率扫描都保留了 CSV。
- **字段方案二次确认（用户）**：小信号去掉 `average_count` 列；**杂散不再判断/标记谐波，落入谐波频率窗的候选直接剔除、不输出**；字段名补单位后缀 → `carrier_hz` / `spurious_freq_hz`；布尔写 `true`/`false`；"其他的能统一就统一"。
- **新发现（必须拆的命名冲突）** —— 经用户澄清后**结论修正**：三个功率类源（功率扫描 / 最大功率 / 低频最大功率）的 `attenuator_value` **都只是"外接衰减器标称值"**：由人告知、**从不下发到任何仪器**，唯一用途是把仪器读数折算回 DUT 端（`actual_power = measured + att`）。
  - 证据：`power_meter.py` 只有 `__init__/check_errors/set_frequency/set_power_unit/reset/zero/measure_power/get_idn`，**没有 `set_input_attenuation`**，故 `max_power_procedure.py:63-65` 的 `if ... hasattr(power_meter,'set_input_attenuation')` 分支**是死代码**，从未执行。
  - 故命名只需**一列 `ext_att_db`**（外接衰减器标称值）；我上一轮提出的 `pm_input_att_db` 系误判，已撤销。
  - 真正下发到仪器的是**频谱仪内部输入衰减**（`spectrum_analyzer.set_attenuation()`，如杂散 40 dB），保留为 `sa_input_att_db`，属 B 区、仅 SA 类源有。低频最大功率同时具备两者。
  - 附带：`use_attenuator=False` 时现写 `attenuation=0.0`，与"0 dB 衰减器"混淆 → 新规范下未使用则**留空**。
- **测量仪器归属（影响字段归属）**：频谱仪 ← 小信号 / 杂散 / 谐波 / 分谐波 / **低频最大功率**；功率计 ← **最大功率** / **功率扫描**。注意 `power_sweep_base` 被"频谱仪类的低频最大功率"和"功率计类的最大功率"共同继承，故其衰减字段语义按子类而异。
- **字段方案 + 只出 CSV 正式落地（本轮执行完成，Task #10-#15 全部关闭）**：
  - `configs/spurious_config.py`：`harmonic_config` → `harmonic_exclusion`（仅 `orders` + `tolerance_hz=5e5`），删 `output.include_harmonics`，`get_config()` 导出 `harmonic_exclusion`；频率边界注释改为"仅用于远段裁剪 + 谐波频率窗剔除"。
  - `procedures/spurious_procedure.py`：类常量 `TEST_TYPE="spurious"` / `FIELDNAMES`（22 列）；`_measure_harmonics` 整块删除、`_classify_harmonic` → `_is_harmonic_spur`（布尔过滤器，在 `_scan_segment` 精测前剔除）；`start_csv_stream` 用 `derive_run_id`；主循环 `include_harmonics` 分支 / `is_harmonic` / `harmonic_order` / JSON `validation_notes` / 旧列名全部清除，改为新 FIELDNAMES 行；`_validate_candidate` 返回 `(status{OK,SUSPECT,SKIP}, flat, note)`，验证指标拍平为 `att_dbc_range_db` / `rbw_scaling_range_db` / `stability_std_db`；删 `import os/json`。
  - `procedures/max_power_procedure.py`：删 `set_input_attenuation` 死分支；步进 status 英文化 OK/SATURATED/OVERLOAD/LIMIT/MEAS_FAIL；新增 `max_set_power_at_peak` 追踪（取到最大实际功率时的设定值）；`ext_att_db` 显式入参。
  - `procedures/power_sweep_base.py`：整file重写 → 双 CSV（`DETAIL_FIELDNAMES` 流式逐点 / `SUMMARY_FIELDNAMES` 结束时一次写）；`sa_context` 机制让子类把 `sa_*` 条件合并进每行；`_finite()` 过滤 None/-inf/nan；`bool_str` 输出 true/false；删 openpyxl/pandas 全部路径。
  - `procedures/low_freq_max_power_procedure.py`：`TEST_TYPE="low_freq_max_power"`，覆写 SUMMARY/DETAIL_FIELDNAMES 加入 `sa_*` 列，在 `run_test` 填充 `self.sa_context`。
  - `procedures/power_sweep_procedure.py`、`small_signal_procedure.py`（19 列，删 `average_count`、新增 `sa_att_mode`）、`harmonic_test_procedure.py`（21 列，改长表加 `harmonic_order`）、`subharmonic_test_procedure.py`（20 列，长表）全部改新字段；`print_summary` 去 pandas 化。
  - `procedures/base_test_procedure.py`：删 `save_results_to_excel` / `finish_xlsx` / `save_results`(xlsx 分支)；新增 `derive_run_id`（**此前被引用但根本不存在**，属潜伏缺陷，已补）、`finish_csv`、`bool_str`；`save_results` 收敛为仅 CSV；`print_summary` 去 pandas → **基类已完全不再依赖 pandas**。
  - `utils/csv_streamer.py`：删 `to_xlsx`，保留逐行 append+flush。
  - 7 个入口 `run_scripts/*.py`：输出路径改 `.csv`（单表 `<tag>_<ts>.csv`；最大功率/低频最大功率 `<tag>_detail_<ts>.csv` + `<tag>_summary_<ts>.csv`），结尾统一 `finish_csv()`；`power_sweep.py` 补上此前**从未调用**的 `start_csv_stream`。
  - `run_scripts/gui.py`：`_save_results` 改 CSV（去 pandas），中文表头映射改新键；新增 `_open_csv` 并接入 5 处 `_exec_*`。
  - 验证：全项目 `ast.parse` 通过；AST 静态比对 **6 个流程的行字典键与其 FIELDNAMES 完全相等**；桩测试跑通功率类双表与杂散端到端（含 `_is_harmonic_spur` 断言、输出中无 2 GHz/3 GHz 谐波、无载波保护带内频点）。
- **未改动**：`harmonic_cable_loss_compensation.py`（按列位置读旧谐波输出、硬编码固定 xlsx，一次性历史脚本）、`wideband.py`（独立绘图脚本）。
- 遗留：`far_carrier_segment.coverage_hz=2e9` 只覆盖 CF ±1 GHz（非全频段），是否扩大仍待用户决定。
- **探明正式运行环境并完成真机验证**（用户问"我有正式环境，你可以调用吗"）：
  - 定位正式解释器：`F:\miniconda\envs\SG_TEST\python.exe`（conda env `SG_TEST`）—— pyvisa 1.16.2 / pyvisa-py / numpy 2.4.6 / pandas 3.0.3 / openpyxl 齐全；NI-VISA 已装（`C:\Windows\system32\visa32.dll`）。
  - 同时排除：`F:\miniconda\python.exe`(base)、`F:\python\python.exe`、托管 Python —— 三者均零依赖。
  - 真实验证（非桩测试）全通过：23 个模块 import 零失败；杂散端到端用**真实 numpy** 跑通且输出无谐波/无载波保护带内频点；功率类双 CSV 正常；运行时逐行键集合与 FIELDNAMES 完全相等。
  - 真机枚举 7 个 VISA 资源；`*IDN?` 只读识别：**Keysight N9030B（MY60089385, A.36.22）在线**（3 条别名路由：`K-N9030B-89385.local` hislip0/inst0、`169.254.212.36` hislip0）；R&S FSW67（`FSW67-101914`）与 `169.254.1.45` 无响应；`ASRL4`/`ASRL7` 串口未探测。
  - 真机验证品牌识别：门面在真实 N9030B 上判定 `keysight` → `KeysightSpectrumAnalyzer` 后端；离线 4 个样例（R&S/Keysight/Agilent/未知）判定全部正确。
  - 确认构造链安全：`SpectrumAnalyzer.__init__` 只发 `*IDN?`，后端 `__init__` 不发任何命令 → 可安全实例化。
  - 已向用户说明：只读查询可自由执行；涉及 RF 输出/功率/衰减等状态变更必须用户明确确认；`run_scripts` 含 `input()` 交互，非交互 shell 无法直接驱动。
- **澄清 `far_carrier_segment.coverage_hz` 的实质问题（用户问"到底出了什么问题"）**，用真实计算列出各载波实际覆盖：
  - `coverage_hz=2e9` + `span_hz=1e9` → 每载波只扫 **CF ±1 GHz（合计 2 GHz）**，占仪器可用频段（9 kHz–40 GHz）**仅 5%**。CF=1/2/6/10/20/30 GHz 均为 2 段；CF=40 GHz 因 `max_frequency_hz` 夹逼 + 重合去重只剩 **1 段（39–40 GHz）**，上下不对称。
  - **关键推论 1（最重要）**：载波 > 2 GHz 时，**次谐波 CF/2 落在扫描范围之外**（需 |CF−CF/2| ≤ 1 GHz ⟺ CF ≤ 2 GHz）。用户明确关心次谐波 → 现配置下杂散流程实质上抓不到次谐波，只能靠独立的 `subharmonic_test_procedure`。谐波已按规则剔除，故"范围窄"只影响非谐波杂散与次谐波。
  - **关键推论 2（新发现的缺陷）**：`output.max_candidates_per_segment = 15` **名字与实现不符** —— `run_spurious_test` 里是 `all_candidates.sort(按幅度降序)[:15]`，即**全局 top-15**，不是每段 15 个。当前 ±1 GHz（2–3 段）勉强能用；一旦放宽覆盖到全频段（~40 段/载波），远段真实杂散会被整体丢弃（绝对幅度最高的一般靠近载波）。
  - 覆盖范围待用户按规范确定，三个候选方向：① 保持 ±1 GHz（只考核近端杂散）② 扫全频段 9 kHz–40 GHz（每载波 ~40 段，时间 ~20×，但架构本身是"宽 RBW 粗扫 → 窄 RBW 精测"两步结构，用 100 kHz–1 MHz RBW 粗扫可行）③ 按规范给定的多窗口偏移列表。
- **修 `max_candidates_per_segment`（用户"第4条需要改"）**：由"全局 top-N"改为**真·按段截断**（`_scan_segment` 精测后 `refined_list.sort()[:max_per_segment]`），主循环全局截断删除。顺带修一个连带缺陷：载波落在 `_scan_segment` 的 `guard` 之外但粗扫分辨率（1 GHz/1001 点 ≈ 1 MHz）远宽于 `carrier_guard_hz`(10 kHz)，载波所在 bin **必然**走到精测后才被丢弃 → 会白占一个"每段 top-N"名额。已加"精测后按实测峰频复审 guard"（L565）。验证：`wb_per_segment_check.py` PASS、`wb_real_env_check.py`（正式环境真实 numpy）PASS。
- **诊断 1 GHz 远段"底噪 -60 dBm 是鸡肋"的根因**（用户提出，尚未定案）：
  - 代码事实：控制台那行 `噪声底 {noise_floor:.1f} dBm`（`spurious_procedure.py:526`）= `_estimate_noise_floor(trace)` = **`np.median(整条迹线)`**（L89-92），**不是 DANL**。该段采集条件（`spurious_config.py:74-83`）：span 1 GHz / RBW 10 kHz / sweep_count 3 / **trace_mode MAXH** / **detector POS**。
  - **驱动从未调用 `set_sweep_points`**（全仓库只有定义无调用）→ X 系列 Preset 默认 **1001 点**（手册原句 "if per default, the number of Trace points is 1001"）→ 1 GHz span 下每点 ≈ **1 MHz**，而 RBW 仅 10 kHz → 每显示点含 ~100 个分辨率单元。
  - 三重抬高：① POS 检波取 bin 内噪声**峰值包络**（高于平均噪声）② 3 次 MAXH 再取跨扫最大 ③ 每点 100 个 RBW 单元，POS 取最大 → 合计 +10 dB 量级。但 RBW 10 kHz 在 10–20 GHz 的 DANL 也只有 -105 ~ -110 dBm 量级，**-60 与它差 45~50 dB** → 差额不可能由噪声解释，必有真实能量或参数未生效。
  - 结构性后果（"鸡肋"的实质）：选峰门限 = `median + noise_margin_db(6 dB)`。若 median 真在 -60，则门限 -54 dBm → **该段低于 -54 dBm 的真杂散既不入选也不精测、直接消失**；远段的实际能力退化为"只报强杂散"，且不能用于任何灵敏度声明。
  - 首要假设（可验证，无需碰仪器）：**RBW 写值未真正生效**（被耦合/设置冲突覆盖，实际可能停在 auto 的宽带 RBW）→ 需加 `BAND:RES?` 读回核对；其次才是 POS/MAXH 抬高。控制台打印的是**请求值**（来自 config）而非读回值，所以打印"RBW 10 kHz"不能证明仪器真的在 10 kHz。
  - 关键量化（决定方案）：扫频 SA 的 **sweep time ∝ span/RBW²，与点数几乎无关** → 点数 1001→10001 基本不增时间（只是迹线更细，POS 偏差从 ~+7 dB 降到 ~+4 dB）；**RBW 才是时间主因**（10 kHz→1 MHz 放宽 100 倍 → 扫时降 10000 倍）。故"全频段覆盖 + 可接受时间"的唯一出路是远段用大 RBW，代价就是灵敏度掉到 -60 ~ -70 dBm 量级 —— 正好等于用户看到的数。
  - 另注：CSV 每行的 `sa_noise_floor_dbm` 取自**精测**（1 MHz span / 100 Hz RBW / WRITE），比控制台那行好，但仍受 POS 影响。
  - 注意：`coverage_hz` 保持 2e9 **未改**（用户"我现在没法确认"）；未触碰任何仪器（用户正在跑数据）。

