# 2026-09-10

- 修复谐波测试摘要打印单位错误：
  - `procedures/harmonic_test_procedure.py` 的 `print_summary()` 改用 `format_frequency()` 基于 `frequency_hz` 输出频率范围、最佳/最差抑制频点，避免低频点显示为 0 MHz。
  - 同步修复 `procedures/subharmonic_test_procedure.py` 的频率范围输出。
- 优化谐波/分谐波测试扫描等待与刻度配置：
  - `instruments/spectrum_analyzer.py` 的 `wait_for_sweep()` 新增 `factor`、`margin` 可选参数，默认仍按 SPAN 自动保留原余量（杂散测试不受影响）。
  - `configs/harmonic_test_config.py` 的 `SPECTRUM_ANALYZER_CONFIG` 新增 `scale_div_db` 和 `sweep_sync_kwargs={'factor': 1.0, 'margin': 0.2}`。
  - `procedures/base_test_procedure.py` 的 `setup_spectrum_analyzer()` 支持按配置设置 `scale_div_db`；`_sweep_sync()` 与 `measure_fundamental_power()` 支持透传同步参数。
  - `procedures/harmonic_test_procedure.py` 所有频率打印改用 `format_frequency()`，并使用 `sweep_sync_kwargs` 减小等待余量，提升小 SPAN 谐波测试速度。
  - `procedures/subharmonic_test_procedure.py` 所有频率打印改用 `format_frequency()`。
